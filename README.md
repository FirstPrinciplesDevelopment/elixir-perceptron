# elixir-perceptron

An Elixir perceptron implemented in a Livebook.

# Running

## Docker

1. Run `docker compose up` and use the link in the command output to connect to livebook in your browser.
2. The livebook file `perceptron.livemd` is mounted under `/data/` in the docker container.
